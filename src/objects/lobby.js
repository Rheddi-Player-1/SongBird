class Lobby{
    name;
    tags;
    datetime;
    uuid;

    constructor(name, tags, datetime, uuid) {
        this.name = name;
        this.tags = tags;
        this.datetime = datetime;
        this.uuid = uuid;
    }
}
